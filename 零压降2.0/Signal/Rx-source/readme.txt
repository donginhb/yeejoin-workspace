-----------------------------------------------
RX-NG-0.08修复bug：
1、写flash时概率性的被狗咬死

-----------------------------------------------
RX-NG-0.07改进：
1、改进uart2接收到的数据的处理
2、用来记录时间的变量以1s为单位，这样可以表示约135年的时间长度

-----------------------------------------------
RX-NG-0.06添加新功能：
1、启用独立看门狗和窗口看门狗
2、添加查看系统运行时间的命令


-----------------------------------------------
RX-NG-0.05修复bug：
1、应该先初始化串口缓冲区


-----------------------------------------------
RX-NG-0.03改进：
1、使没有接收到数据的条件更加严格
2、优化调试接口的命令处理

=================================================================================================================
RX-NG-0.02修复bug：
1、保存15分钟时的温度控制参数值，导致上电后，10多分钟的加热，延迟读signal_cfg_param.temper_pwm可以读取正确数据。


=================================================================================================================
RX-NG-0.01新增功能：
//1、增加与信号采集端的同步机制（因此，该版本不能与采集端的老版本软件配套使用） ====  ==== 接收端使用6字节dma时，波形不好，所以取消
1、插点 === 未实现
2、增加系统维护串口功能
3、添加了保存温度pwm的功能（保存上电15分钟后的值）
4、添加进入、退出参数设定模式的命令

RX-NG-0.01改进：
1、接收使用dma-2字节


================================================================================================================
================================================================================================================
================================================================================================================
================================================================================================================
2011.[UART_Int_D_ID]VER4.0.0_VBT6[接收TEST_SPI_DMA_0414]